You are a senior full-stack engineer working on this existing project.

IMPORTANT:

* Do NOT change the database structure.
* Do NOT create new tables.
* Do NOT rename existing roles.
* Keep all current functionality working.
* Apply authorization changes only.
* Ensure the application remains fully compatible with Supabase and Vercel deployment.

Current roles:

* administrator
* admin
* staff

Required permissions:

1. administrator

* Full access to all features and settings.
* No restrictions.

2. admin
   MUST NOT be able to access, view, edit, create, delete, or manage:

* Organization / Clinic Name settings
* Lists Management
* Restore Points
* Backup functionality
* Warehouse
* Reset password for other users

Admin should continue to have access to all other permitted features.

3. staff
   MUST NOT be able to access, view, edit, create, delete, or manage:

* Organization / Clinic Name settings
* Lists Management
* Restore Points
* Backup functionality
* Near-Expiry Warning Days setting

Staff should continue to have access only to their currently allowed operational features.

Implementation requirements:

* Update both frontend UI permissions and backend authorization checks.
* Hide restricted menu items, pages, buttons, actions, and API operations.
* Prevent direct URL access to restricted pages.
* Prevent unauthorized API/database operations even if a user bypasses the UI.
* Reuse the existing role system and permission architecture.
* Do not introduce breaking changes.

Verification checklist:

* administrator can access everything.
* admin cannot access:

  * Organization / Clinic Name
  * Lists Management
  * Restore Points
  * Backup
  * Warehouse
  * Reset password for other users
* staff cannot access:

  * Organization / Clinic Name
  * Lists Management
  * Restore Points
  * Backup
  * Near-Expiry Warning Days

Deployment requirements:

* Review the entire project for Vercel deployment readiness.
* Fix any TypeScript, ESLint, build, routing, environment variable, or runtime issues that could cause Vercel deployment failures.
* Verify all Supabase calls, policies, authentication flows, and environment variables.
* Verify Vercel configuration files.
* Verify production build succeeds with zero errors.
* Verify there are no missing imports, unused exports causing build failures, or server/client boundary issues.

Final output required:

1. List every file changed.
2. Explain each permission change.
3. Explain any Supabase changes required.
4. Explain any Vercel changes required.
5. Confirm whether database migrations are required. If not, explicitly state: "No database schema changes required."
6. Provide a final deployment readiness report with any remaining risks.
